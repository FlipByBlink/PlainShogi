import SwiftUI

struct ContentView: View {
    var body: some View {
        将棋View_tvOSApp()
            .modifier(💾アクティブ復帰時にiCloudを明示的に同期())
            .environment(\.layoutDirection, .leftToRight)
    }
}
